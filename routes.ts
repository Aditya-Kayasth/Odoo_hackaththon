export const publicRoutes = ["/", "/emailVerification","/passwordResetEmail","/passwordResetForm"];

export const authRoutes = ["/login", "/register"];

export const apiAuthPrefix = "/api/auth";

export const Loginredirect = "/User/1";
